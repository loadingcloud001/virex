# Copy this to `.env` and fill in the real Hikvision camera passwords.
# `.env` is gitignored — never commit it.
#
# Get the real password from your Hikvision camera config or the
# installer notes.

RTSP_PASSWORD_1=replace_with_password_for_218_189_218_174
RTSP_PASSWORD_2=replace_with_password_for_210_3_7_254