# Utility Scripts

Helper scripts for common operations.

## Planned Scripts

- `init_db.py` - Initialize database with schema (TBD - Week 1)
- `seed_data.py` - Insert test tenant + cameras (TBD - Week 2)
- `health_check.sh` - Check all services are healthy (TBD - Week 6)

## Usage

```bash
# Initialize database
python scripts/init_db.py

# Seed test data
python scripts/seed_data.py

# Health check
./scripts/health_check.sh
```